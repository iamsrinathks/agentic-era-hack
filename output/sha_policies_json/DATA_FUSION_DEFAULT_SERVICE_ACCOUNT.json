{
  "display_name": "DataFusion Default Service Account",
  "description": "Detects Data Fusion instances that are using the default Compute Engine service account, which often violates the principle of least privilege.",
  "custom_config": {
    "predicate": {
      "expression": "resource.serviceAccount.endsWith(\"compute@developer.gserviceaccount.com\")"
    },
    "resource_selector": {
      "resource_types": ["datafusion.googleapis.com/Instance"]
    },
    "severity": "HIGH",
    "recommendation": "Assign a dedicated service account with the principle of least privilege to Data Fusion instances instead of the default Compute Engine service account."
  }
}